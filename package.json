{
  "name": "Valorium_currency",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix src/frontend run build",
    "start:replica": "dfx start --clean --background",
    "deploy": "dfx deploy",
    "dev": "npm --prefix src/frontend run dev",
    "setup": "npm install && npm --prefix src/frontend install",
    "stop": "dfx stop"
  }
}